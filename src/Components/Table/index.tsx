import SearchBar from "./SearchBar";
import TableContent from "./TableContent";

const Table = () => {
    return (
        <div>
            <SearchBar />
            <TableContent />
        </div>
    );
};

export default Table;
